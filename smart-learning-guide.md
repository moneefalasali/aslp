# دليل تشغيل وفهم نظام التعلم الذكي (Smart Learning System)

هذا الدليل يشرح آلية عمل التطبيق، الواجهات المتوفرة، وكيفية ربط الخدمات الخارجية وتشغيل النظام بالكامل.

---

## 1. آلية عمل التطبيق
يعتمد التطبيق على تقنيات الذكاء الاصطناعي لتحويل المحتوى التعليمي (ملفات PDF أو تسجيلات صوتية) إلى مواد ملخصة واختبارات تفاعلية. تمر العملية بثلاث مراحل أساسية:

| المرحلة | الوصف | التقنية المستخدمة |
| :--- | :--- | :--- |
| **الرفع والتخزين** | يتم رفع الملف إلى خادم سحابي وتخزين بياناته في قاعدة البيانات. | Wasabi S3 / SQLAlchemy |
| **استخراج النصوص** | استخراج النص من ملفات PDF أو تحويل الصوت إلى نص مكتوب. | PyPDF2 / OpenAI Whisper |
| **التحليل الذكي** | تحليل النص لاستخراج الملخص، النقاط الرئيسية، وإنشاء أسئلة اختبار. | OpenAI GPT-4o |

---

## 2. التحقق من الواجهات (Interfaces)
تم التحقق من وجود جميع الواجهات المطلوبة وهي تعمل بتوافق تام مع الواجهة الخلفية:

### واجهات المستخدم (User Interfaces):
*   **الصفحة الرئيسية (`/`):** تعرض مميزات النظام وتسمح بالانتقال لرفع الملفات.
*   **صفحة تسجيل الدخول/الاشتراك (`/login`, `/register`):** نظام كامل لإدارة الحسابات.
*   **لوحة رفع الملفات:** تدعم السحب والإفلات لملفات PDF والصوت (MP3, WAV, M4A).
*   **واجهة النتائج:** تعرض الملخص، النقاط الرئيسية، واختبار تفاعلي مع حساب النتيجة.
*   **سجل العمليات (`/history`):** يستعرض جميع الملفات التي تم تحليلها سابقاً.

### واجهات الإدارة (Admin Interfaces):
*   **تسجيل دخول الإدارة (`/admin-login`):** واجهة خاصة لمديري النظام.
*   **لوحة تحكم الإدارة (`/admin-dashboard`):** تعرض إحصائيات النظام (عدد المستخدمين، عدد الملفات) وإدارة المستخدمين.

---

## 3. كيفية ربط الـ API والخدمات الخارجية
يتم ضبط الإعدادات في ملف `config.py` أو عبر متغيرات البيئة (`.env`):

### أ. OpenAI API (للتحليل وتحويل الصوت):
1. احصل على المفتاح من [OpenAI Dashboard](https://platform.openai.com/).
2. قم بتعيين `OPENAI_API_KEY`.

### ب. Wasabi S3 (لتخزين الملفات):
1. أنشئ حساباً في Wasabi وقم بإنشاء Bucket جديد.
2. قم بتعيين المتغيرات التالية:
    *   `WASABI_ACCESS_KEY`
    *   `WASABI_SECRET_KEY`
    *   `WASABI_BUCKET_NAME`
    *   `WASABI_REGION`

---

## 4. تشغيل النظام
لضمان عمل النظام بكفاءة، اتبع الخطوات التالية:

### المتطلبات التقنية:
*   Python 3.10+
*   قاعدة بيانات (SQLite افتراضياً، أو PostgreSQL للإنتاج).

### خطوات التشغيل:
1.  **تثبيت المكتبات:**
    ```bash
    pip install -r requirements.txt
    ```
2.  **إعداد قاعدة البيانات:**
    سيقوم التطبيق بإنشاء ملف `smart_learning.db` تلقائياً عند أول تشغيل.
3.  **تشغيل الخادم:**
    ```bash
    python app.py
    ```
    سيعمل التطبيق على الرابط: `http://localhost:8000`

---

## ملاحظات تقنية هامة (تم إصلاحها):
*   **إصلاح الاستيراد:** تم تصحيح مسارات استيراد `auth_service` في خدمات المستخدم.
*   **ديكوريتور الحماية:** تم إضافة `token_required` لضمان حماية المسارات الحساسة.
*   **التوافق:** تم التأكد من أن جميع طلبات الواجهة الأمامية (JavaScript) تتوافق مع مسارات الـ API في Flask.
